{
  "name": "Simulation",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspaces/${localWorkspaceFolderBasename}/.venv/bin/python",
        "jupyter.defaultKernel": "python3",
        "jupyter.askForKernelRestart": false,
        "jupyter.askForKernelSelection": false
      }
    }
  },

  "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt ipykernel && python -m ipykernel install --prefix .venv --name python3 --display-name \"Python 3 (venv)\""
}
