{
  "name": "Simulation",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "jupyter.askForKernelDisplayName": false,
        "jupyter.defaultInterpreter": "/usr/local/bin/python"
      }
    }
  },

  "postCreateCommand": "pip install -r requirements.txt $$ pip install jupyter ipykernel && python -m ipykernel install --user --name my_env --display-name 'Python (My Env)'"
}
